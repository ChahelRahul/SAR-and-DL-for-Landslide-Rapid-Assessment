model:
  version: v2
  orbit: ASCENDING
  probability_threshold: 0.6
  nms_overlap: 0.1
  batch_size: 512

imagery:
  pre_days: 60
  post_days: 12
  scale_m: 10

processing:
  tile_size: 64
  overlap: 0.5
  max_roi_km2: 10000
  resolution_tolerance: 0.2
  max_nodata_fraction: 0.25
  value_min_db: -60.0
  value_max_db: 30.0

output_dir: outputs
